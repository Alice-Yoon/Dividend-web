import React from 'react';
import styled from 'styled-components';

function CompanyDetails(props) {
    return (
        <div className={props.className}>
            CompanyDetails- 종목 상세 페이지
        </div>
    )
}

export default styled(CompanyDetails)`

`;
